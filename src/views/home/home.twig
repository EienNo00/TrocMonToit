<!DOCTYPE html>
<html lang="en">

<head>
    <title>Home</title>
</head>

<body>
    <button>profil</button>

    <div class="container-filter">
        {% for type in types %}
        <a class="filtre_btn" href="?types={{ type }}">{{ type }}</a>
        {% endfor %}
        <a class="filtre_btn" href="/home">reset</a>
    </div>




    <div class="house-container" style="display: flex; flex-wrap: wrap; justify-content: center;">
        {% for housing in housings %}
        <div class="house">
            <p class="Name">{{ housing.types }} {{ housing.name }}</p>
            <div class="bottom"
                style="display: flex; width: 95%; height: 80%; flex-direction: column;align-items: center;justify-content: flex-end;">
                <p class="description">{{ housing.description }}</p>
                <p>Adresse : {{ housing.address }}</p>
                <p>Prix : {{ housing.price }}$</p>
                <p>Services : {{ housing.services }}</p>
                <p>Equipements : {{housing.equipment}}</p>
            </div>
        </div>
        {% endfor %}
    </div>
</body>
<style>
    .filtre {
        margin: 5px;
    }

    .description {
        color: gray;
        font-size: small;
        border: #222 solid 2px;
        border-radius: 2px;
        height: 8vh;
        width: 35vh;
        overflow: hidden;
    }

    .Name {
        font-size: 25px;
        font-weight: bold;
    }

    html {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        background-color: rgb(227, 227, 227);
        height: 100%;
    }

    body {
        margin: 5px;
    }

    .house-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        max-height: 85%;
    }

    body::-webkit-scrollbar {
        display: none;
    }

    .house-container::-webkit-scrollbar {
        display: none;
    }

    p {
        margin: 2px;
    }

    button {
        background-color: #222;
        border-radius: 4px;
        border-style: none;
        box-sizing: border-box;
        color: #fff;
        cursor: pointer;
        display: inline-block;
        font-family: "Farfetch Basis", "Helvetica Neue", Arial, sans-serif;
        font-size: 16px;
        font-weight: 700;
        line-height: 1.5;
        margin: 0;
        max-width: none;
        min-height: 44px;
        min-width: 10px;
        outline: none;
        overflow: hidden;
        padding: 9px 20px 8px;
        position: relative;
        text-align: center;
        text-transform: none;
        user-select: none;
        -webkit-user-select: none;
        touch-action: manipulation;
        width: 20vh;
    }

    a {
        background-color: #222;
        border-radius: 4px;
        border-style: none;
        box-sizing: border-box;
        color: #fff;
        cursor: pointer;
        display: inline-block;
        font-family: "Farfetch Basis", "Helvetica Neue", Arial, sans-serif;
        font-size: 16px;
        font-weight: 700;
        line-height: 1.5;
        margin: 5px;
        max-width: none;
        min-height: 44px;
        min-width: 10px;
        outline: none;
        overflow: hidden;
        padding: 9px 20px 8px;
        position: relative;
        text-align: center;
        text-transform: none;
        user-select: none;
        -webkit-user-select: none;
        touch-action: manipulation;
        width: auto;
    }

    .house {
        overflow: hidden;
        color: black;
        border-radius: 15px;
        height: 40vh;
        width: 40vh;
        background-color: rgb(227, 227, 227);
        margin: 9px;
        display: flex;
        align-items: center;
        flex-direction: column;
        transition: all .2s ease-in-out;
        box-shadow: rgba(255, 255, 255, 0.1) 0px 1px 1px 0px inset, rgba(50, 50, 93, 0.25) 0px 50px 100px -20px, rgba(0, 0, 0, 0.3) 0px 30px 60px -30px;
    }

    .house:hover {
        overflow: auto;
        transform: scale(1.5);
    }
</style>

</html>